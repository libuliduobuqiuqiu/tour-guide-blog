NODE_ENV=production
API_URL=https://yourdomain.com
# 如果你希望前端构建后写死域名，可设置该值；否则留空使用相对路径
# NEXT_PUBLIC_API_URL=https://yourdomain.com
PORT=3000

# 前端日志文件（systemd 启动时会自动创建目录）
FRONTEND_LOG_FILE=/var/log/tour-guide/frontend.log
